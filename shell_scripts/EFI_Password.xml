<?xml version="1.0" encoding="UTF-8"?>
<extensionAttribute>
    <displayName>EFI Password</displayName>
    <displayInCategory>Security Reporting</displayInCategory>
    <dataType>string</dataType>
    <description>Displays "Set" if a firmware password is set; displays "Not Set" otherwise.</description>
    <scriptContentsMac>
        #!/bin/sh

        setregproptool -c
        result = $?
    
        if [[ "$result" == "0" ]]; then
            echo "&lt;result&gt;Set&lt;/result&gt;"
        else
            echo "&lt;result&gt;Not Set&lt;/result&gt;"
        fi
    </scriptContentsMac>
    <scriptContentsWin></scriptContentsWin>
</extensionAttribute>